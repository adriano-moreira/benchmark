namespace CSharpProject.Modules
{
    public class RuleOfTree
    {
        static void Exec()
        {
            var totalApples = 3;
            var totalValue = 10;
            var totalWithOutPrice = 2;
            var totalPrice = (totalValue / totalApples) * totalWithOutPrice;
        }
    }
} 